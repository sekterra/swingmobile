var backEndUrl = 'localhost'
var backEndPort = '8080'

var config = {
  themeClasses: {
    search: "blue lighten-5"
  },
  port: 9090,
  backEndUrl: backEndUrl,  // backend url
  backEndPort: backEndPort, // backend port
  protocol: 'http://',
  backEndFullUrl : backEndUrl + ':' + backEndPort
}

export default config;